' 漢字にカタカナでフリガナを振る関数
' ===========================================
' 使用方法：
'     1．　標準モジュールに作成
'     2．　=Furigana(文字列)　をセルに入力
' ===========================================
Function Furigana(a As Range) As String
    Furigana = Application.GetPhonetic(a)
End Function
